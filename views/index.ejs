<!doctype html>
<head>
    <title>Attachd - Gmail Attachments Extension</title>
    <link href='http://fonts.googleapis.com/css?family=Roboto:400,700' rel='stylesheet' type='text/css'>
    <style>
        body {
            background: #ddd;
            font-family: "Roboto", sans-serif;
        }

        .page {
            margin: 2em auto;
            background: #fff;
            padding: 1em 2em;
            box-shadow: 0 5px 15px #888;
            width: 50%;
            border-radius: 3px;
        }

        a {
            text-decoration: none;
            color: #fff;
            background: #dd4b39;
            padding: 0.75em 1.5em;
            display: inline-block;
        }

        a:hover {
            background: #e74b37;
            box-shadow: inset 0 -2px 0 rgba(0, 0, 0, 0.2);
        }

        a:active {
            background: #be3e2e;
            box-shadow: inset 0 2px 0 rgba(0, 0, 0, 0.2);
        }

        a, img {
            border-radius: 4px;
        }

        .google {
            vertical-align: -16px;
            padding-right: 1em;
        }
    </style>

</head>
<body>
    <div class="page">
        <% if (user) { %>

            <h1>Welcome <%= user.name %>!</h1>
            <p><img style="width: 100px;" src="<%= user.picture %>" /></p>
            <p>Your account is ready to be used, we are currently scanning attachments from your Gmail account, <b><%= user.email %></b>.</p>
            <p>Install our Chrome extension to find attachments quickly within Gmail.</p>
            <p><a href="/deauth?redirect=true">Logout</a></p>

        <% } else { %>

            <h1>Welcome to Attachd!</h1>
            <p><a href="/auth/google"><img class="google" src="google.png" /> Login with Google to start using Attachd</a></p>

        <% } %>
    </div>
</body>
